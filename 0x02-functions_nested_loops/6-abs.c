#include "main.h"

/**
 * _abs - calculates the absolute value of an integer
 * @i: the integer to calculate
 *
 * Return: the absolute value of int
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}

