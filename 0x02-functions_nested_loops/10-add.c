#include "main.h"
/**
 * add - operation that adds two integer values
 * and returns the result
 *
 * @x: first number
 * @y: second number
 *
 * Return: results
 */
int add(int x, int y)
{
	return (x + y);
}
